g = {'CSC300': ['CSC100', 'CSC200'], 'CSC200': ['CSC100'], 'CSC100': []}
n = {}
result = []
for i in g:
    n[len(g[i])] = i

if sorted(n.keys()) == list(range(len(g))):
    for i in sorted(n.keys()):
        result.append(n[i])
result
